import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Define reporting state structure here
};

const reportingSlice = createSlice({
  name: 'reporting',
  initialState,
  reducers: {},
});

export default reportingSlice.reducer;